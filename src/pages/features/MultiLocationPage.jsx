import FeatureTemplate from '../../components/features/FeatureTemplate';
import { featuresData } from '../../data/featuresData';

const MultiLocationPage = () => {
  return <FeatureTemplate feature={featuresData['multi-location']} />;
};

export default MultiLocationPage;
