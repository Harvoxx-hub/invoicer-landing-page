import FeatureTemplate from '../../components/features/FeatureTemplate';
import { featuresData } from '../../data/featuresData';

const POSPage = () => {
  return <FeatureTemplate feature={featuresData.pos} />;
};

export default POSPage;
