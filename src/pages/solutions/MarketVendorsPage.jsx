import SolutionTemplate from '../../components/solutions/SolutionTemplate';
import { solutionsData } from '../../data/solutions';

const MarketVendorsPage = () => {
  return <SolutionTemplate solution={solutionsData['market-vendors']} />;
};

export default MarketVendorsPage;
